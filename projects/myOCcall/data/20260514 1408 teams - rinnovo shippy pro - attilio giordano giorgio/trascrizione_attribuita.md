# Trascrizione attribuita — non disponibile

L’attribuzione per parlante non è stata ricostruita in questa run perché non risultano `speaker-events.jsonl` / metadati DOM sufficienti.

Usare:
- `trascrizione.txt` per i timestamp audio
- `transcripts/transcript-events.jsonl` per l’allineamento temporale
- `SINTESI.md` per la sintesi decisionale e i partecipanti ricostruibili
