# Sessione 2026-05-14 — myOCcall Speaker Attribution v1

## Cosa abbiamo fatto

### Revisione TODO myOCcall
- Atti ha chiesto di vedere i task aperti del progetto myOCcall
- Priorità concordata: **Speaker Attribution v1** su Google Meet, poi affinamento summarizer

### Implementazione Speaker Attribution v1

Commit: `1ae2646` — tutti i file in `projects/myOCcall/scripts/`

**Modifiche:**

1. **`call-start.sh`** — aggiunge `ffmpeg_start_epoch_ms` (epoch Unix ms) a META.md per allineamento clock
2. **`call-join-meet.js`** — speaker monitoring ogni 2s con 4 strategie DOM Meet in cascata:
   - `[data-is-speaking="true"]` diretto sul tile
   - tile figlio con `data-is-speaking`
   - `[data-is-main-video="true"]` (spotlight)
   - caption speaker (`[jsname="wWt0nb"]`)
   → scrive `speaker-events.jsonl` ad ogni cambio parlante
   → registra `bot_join_epoch_ms` in META.md
3. **`call-transcribe-segments.sh`** — switch da `response_format=text` a `verbose_json`:
   - salva `segment-XXXX.json` con timestamp Whisper per segmento
   - genera `transcripts/transcript-events.jsonl` con `call_start_sec`/`call_end_sec`
4. **`call-speaker-overlay.py`** (nuovo) — allinea `speaker-events.jsonl` ↔ `transcript-events.jsonl`:
   - usa overlap temporale per assegnare parlante a ogni frase
   - produce `trascrizione_attribuita.md` formato `[HH:MM:SS] Nome: testo`
5. **`call-post.sh`** — chiama overlay dopo trascrizione, include parlanti in SINTESI.md

## Da fare prossima sessione myOCcall

- **TEST su call reale** (priorità alta) — verificare selettori DOM Meet, affinare se non matchano
- Retry Whisper con backoff
- Alert audio silenzioso
- Skill `call-summarizer`
- Riconoscimento piattaforma da URL
