- [2026-05-22] Debug cron padel: `projects/myAgenda/scripts/padel_checker.py` completes in ~1.5s when run in isolation; the recurring failure is in the cron/agent wrapper, with overlapping cron sessions and repeated `cron: job execution timed out` errors. One run also showed a Google ICS SSL handshake timeout.
