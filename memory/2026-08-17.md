# 2026-08-17

## Backup notturno OpenClaw FTP (cron 02:00) — completato con incidente

- Primo run (02:00): ucciso dal timeout exec 600s a metà upload FTP (SIGTERM). L'archivio combinato è cresciuto: ora ~7,1 GB (vs 3,5 GB di metà agosto) → upload totale ~20-30 min.
- Rilancio (02:11): completato OK. Verificato file remoto 7.482.076.253 bytes = esattamente l'archivio locale. Cleanup /tmp eseguito. Retention OK (8 file sul server).
- **Token bot Telegram dello script (8699275494:...) NON valido → 401 Unauthorized.** Le notifiche automatiche falliranno ogni notte finché Atti non rigenera il token con @BotFather e aggiorna `_utility/static/openclaw_ftp_backup.py`. Intanto la notifica l'ho inviata io manualmente via canale telegram OpenClaw a chat 506258994 (msg 6742).

### Lezioni per i prossimi run del cron backup
1. **MAI timeout 600s**: lo script richiede 20-30 min. Lanciare in `background: true` e monitorare con `tail /tmp/openclaw_backup_$(date +%Y%m%d).log` + poll del pid.
2. Se il run muore a metà, rimane in /tmp l'archivio parziale e il work dir: pulirli prima del rilancio (il cleanup dello script parte solo a fine run).
3. I timestamp nel log dello script sono tutti identici (bug: `NOW` catturato una sola volta all'import) — fidarsi degli mtime dei file, non del log.
4. Attendersi ~7 GB di archivio: controllare `df -h /` se lo spazio scarseggia.
